# Production Environment Variables  
# Use with: docker compose --env-file .env.production up

# Conservative timeouts for production (5 minutes idle)
WS_PING_INTERVAL=300   # 5 minutes  
WS_PING_TIMEOUT=60     # 1 minute

# Server configuration
DATA_DIR=/data
SERVER1_WS_PORT=8000
CLIENT_WS_PORT=8080
SERVER1_URL=http://server1:8001